<div class="row">
  <div class="col-md-12">
    <h1>{{User.name}}</h1>
  </div>
  <div class="col-md-4">
    <img class="img-responsive center-block" src="{{}}" style="width: 250px;margin-bottom: 25px;">
    <div class="well">
      <ul class="list-unstyled">
        <li><strong>Opening Hour:</strong> {{user.name}}</li>
        <li><strong>Tel:</strong> {{user.email}}</li>
      </ul>
    </div>
  </div>
  <hr>
</div>